import { SharedQueryClauseToken } from "./SharedQueryClauseToken";
export class SharedSelectToken extends SharedQueryClauseToken {
    constructor(modifier) {
        super();
        this.modifier = modifier;
        this.variables = [];
    }
    addVariable(...variables) {
        this.variables.push(...variables);
        return this;
    }
    toString(spaces) {
        let query = `SELECT`;
        if (this.modifier)
            query += ` ${this.modifier}`;
        query += this.variables.length ?
            ` ${this.variables.join(" ")}` :
            " *";
        return query;
    }
}

//# sourceMappingURL=SharedSelectToken.js.map
