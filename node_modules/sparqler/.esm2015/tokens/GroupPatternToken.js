import { getTokenContainerString } from "./printing";
export class GroupPatternToken {
    constructor() {
        this.token = "groupPattern";
        this.patterns = [];
    }
    addPattern(...patterns) {
        this.patterns.push(...patterns);
        return this;
    }
    toString(spaces) {
        return getTokenContainerString({
            spaces,
            tags: { open: "{", close: "}" },
            tokensSeparator: ".",
            tokens: this.patterns,
        });
    }
}

//# sourceMappingURL=GroupPatternToken.js.map
