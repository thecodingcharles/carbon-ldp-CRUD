export class UnionPatternToken {
    constructor() {
        this.token = "unionPattern";
        this.groupPatterns = [];
    }
    toString(spaces) {
        return this
            .groupPatterns
            .map(x => x.toString(spaces))
            .join(` UNION `);
    }
}

//# sourceMappingURL=UnionPatternToken.js.map
