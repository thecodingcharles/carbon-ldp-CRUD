import { GroupPatternToken } from "./GroupPatternToken";
export class ServicePatternToken {
    constructor(resource, modifier) {
        this.token = "servicePattern";
        this.modifier = modifier;
        this.resource = resource;
        this.groupPattern = new GroupPatternToken();
    }
    toString(spaces) {
        let query = `SERVICE `;
        if (this.modifier)
            query += `SILENT `;
        query += `${this.resource} ${this.groupPattern.toString(spaces)}`;
        return query;
    }
}

//# sourceMappingURL=ServicePatternToken.js.map
