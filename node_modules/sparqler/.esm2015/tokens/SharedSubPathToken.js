export class SharedSubPathToken {
    constructor(path) {
        this.token = "subPath";
        this.path = path;
    }
    toString() {
        if (!this.path)
            return "()";
        return `(${this.path})`;
    }
}

//# sourceMappingURL=SharedSubPathToken.js.map
