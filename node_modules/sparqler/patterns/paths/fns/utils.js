"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var SharedSubPathToken_1 = require("../../../tokens/SharedSubPathToken");
function _getTokenWrapper() {
    var symbols = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        symbols[_i] = arguments[_i];
    }
    return function (token) {
        if (token === "a")
            return token;
        if (symbols.indexOf(token.token) !== -1)
            return new SharedSubPathToken_1.SharedSubPathToken(token);
        return token;
    };
}
exports._getTokenWrapper = _getTokenWrapper;
function _isBasePrimitive(token) {
    return token === "a"
        || token.token === "iri"
        || token.token === "prefixedName";
}
exports._isBasePrimitive = _isBasePrimitive;
function _isPathInNegatedToken(token) {
    return _isBasePrimitive(token)
        || (token.token === "pathInverse"
            && _isBasePrimitive(token.path));
}
exports._isPathInNegatedToken = _isPathInNegatedToken;

//# sourceMappingURL=utils.js.map
